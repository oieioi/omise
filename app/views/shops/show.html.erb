<nav> <%= link_to '戻る', shops_path %> </nav>

<h1><%= @shop.name %></h1>

<%= link_to 'edit', edit_shop_path(@shop) %>
<%= link_to 'delete', shop_path(@shop), method: :delete, data: {confirm: "OK?"} %>

<p>
  説明: <%= @shop.description %>
</p>

<p>
  url: <%= @shop.url %>
</p>
